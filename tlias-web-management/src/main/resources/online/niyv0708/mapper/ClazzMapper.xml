<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="online.niyv0708.mapper.ClazzMapper">
    <select id="list" resultType="online.niyv0708.pojo.Clazz">
        select c.*, e.name masterName
#         ,(CASE
#         WHEN c.begin_date > CURDATE() THEN '未开班'
#         WHEN c.begin_date &lt;= CURDATE() AND c.end_date >= CURDATE() THEN '在读'
#         WHEN c.end_date &lt; CURDATE() THEN '已结课'
#         END) as status
        from clazz as c
        left join emp as e on c.master_id = e.id
        <where>
            <if test="name != null and name != ''">
                c.name like concat('%',#{name},'%')
            </if>
            <if test="begin != null and end != null">
                and c.begin_date between #{begin} and #{end}
            </if>
        </where>
        order by  c.id
    </select>
</mapper>
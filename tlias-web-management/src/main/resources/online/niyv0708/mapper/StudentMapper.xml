<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="online.niyv0708.mapper.StudentMapper">
    <insert id="insert">
        insert into student (name, no, gender, phone, degree, clazz_id, id_card, is_college, address, graduation_date, create_time,update_time)
        values (#{name}, #{no}, #{gender}, #{phone}, #{degree}, #{clazzId}, #{idCard}, #{isCollege}, #{address}, #{graduationDate}, #{createTime}, #{updateTime})
    </insert>
    <update id="update">
        update student
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="no != null">
                no = #{no},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="idCard != null">
                id_card = #{idCard},
            </if>
            <if test="isCollege != null">
                is_college = #{isCollege},
            </if>
            <if test="address != null">
                address = #{address},
            </if>
            <if test="degree != null">
                degree = #{degree},
            </if>
            <if test="graduationDate != null">
                graduation_date = #{graduationDate},
            </if>
            <if test="clazzId != null">
                clazz_id = #{clazzId},
            </if>
            <if test="violationCount != null">
                violation_count = #{violationCount},
            </if>
            <if test="violationScore != null">
                violation_score = #{violationScore},
            </if>
        </set>
            where id = #{id}
    </update>
    <delete id="delete">
        delete from student where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <select id="list" resultType="online.niyv0708.pojo.Student">
        select s.*, c.name clazzName
        from student as s
        left join clazz as c on s.clazz_id = c.id
        <where>
            <if test="name != null and name != ''">
                s.name like concat('%',#{name},'%')
            </if>
            <if test="degree != null and degree != ''">
                and s.degree >= #{degree}
            </if>
            <if test="clazzId != null">
                and s.clazz_id = #{clazzId}
            </if>
        </where>
        order by s.id
    </select>
    <select id="getClazzData" resultType="java.util.Map">
        select
            (case clazz_id when 1 then 'JavaEE就业157期'
                         when 2 then 'JavaEE就业158期'
                         when 3 then 'JavaEE就业159期'
                         when 4 then 'JavaEE就业160期'
                         when 5 then 'JavaEE就业161期'
                         when 6 then 'JavaEE就业162期'
                         when 7 then 'JavaEE就业163期'
                         when 8 then 'JavaEE就业164期'
                         when 9 then 'JavaEE就业165期'
                         else '其他' end) clazz,
            count(*) total
        from student
        group by 1  -- 使用第一列进行分组
        order by 2 desc -- 使用第二列进行排序
    </select>
    <select id="countDegreeData" resultType="java.util.Map">
        select
            (case degree when 1 then '初中'
                         when 2 then '高中'
                         when 3 then '大专'
                         when 4 then '本科'
                         when 5 then '硕士'
                         when 6 then '博士'
                         else '其他' end) name,
            count(*) value
        from student
        group by 1  -- 使用第一列进行分组
        order by 2  -- 使用第二列进行排序
    </select>
</mapper>